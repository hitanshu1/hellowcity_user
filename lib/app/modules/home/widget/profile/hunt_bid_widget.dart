import 'package:flutter/material.dart';

class HuntBidWidget extends StatelessWidget {
  const HuntBidWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
